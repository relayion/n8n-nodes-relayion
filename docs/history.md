# n8n-nodes-relayion — Build History

Not loaded by default. Read when you need context on what was built or when a phase was completed.

---

## Completed Phases

### Phase 1 — Initial Build and Release
Completed: 2026-06-25

Built the full n8n community node package from scratch using the `n8n-nodes-starter` template as the base. Implemented two nodes (Relayion regular node and Relayion Trigger), one credential type, a shared transport helper, and four resource modules. Passed a security review before publishing. Published to npm v0.1.1 via GitHub Actions with provenance. End-to-end tested on a live n8n instance — outbound send and inbound trigger both confirmed working.
